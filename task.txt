1. Напишите глобальную функцию getObj(), которая возвращает this.

2. Напишите конструктор для создания объекта Country(title - название страны, capital - столица, population - население, area - площадь).

3. В этом конструкторе создайте метод getCountry(), который ссылается на глобальную функцию getObj().

4. Создайте при помощи конструктора две страны (на ваше усмотрение) со всеми необходимыми свойствами. Можно использовать параметры по умолчанию.

4. Напишите функцию, которая будет в цикле выводить все свойства объекта, кроме методов в консоль в формате key: value. Для вывода свойств должен использоваться метод getCountry().

5. Объект должен передаваться в функцию в параметрах.





////////////////////////////////////


1. Ты создал функцию getCoutry(), кстати пропустил букву "n"  и ключевое слово "function" и поэтому VS Code "ругается", в глобальной области. А нужно было внутри конструктора Country(). 



2. Когда говорится, что метод ссылается на функцию getObj(), имеется ввиду, что он приравнен этой функции, т.е. ссылается на туже область памяти, что и она (this.getCountry = getObj). 



3. Затем нужно написать функцию, которая будет получать объект страны, созданный с помощью конструктора в качестве аргумента (func(country)) и перебирать его в цикле for-in передавая в метод getCountry() ключи объекта (примерно так country.getCountry()[key]). 



Эти сложности нужны в учебных целях чтобы наглядно показать, как работает this.

Рекомендую запускать код на исполнение в debugger DevTools. 

Удачи. 
Если что-то не понятно - пиши. На этой неделе я буду немного свободнее.

/////////////////////////////

Тебе нужно создать еще одну функцию (назовем ее getPropsCountry, к примеру), которая будет получать объект страну в аргументах и затем перебирать ее в цикле. Функция getObj() возвращает this. 
